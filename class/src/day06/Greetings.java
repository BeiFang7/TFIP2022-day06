package day06;

//SAM - Single Abstract Method
public interface Greetings {
  //create a function type, which has one single parameter, and the parameter type is string, and it does not give any return value.
  public void hello(String name);
}
